// SECURITY: DemoRoleSwitcher has been removed for production.
// This component is intentionally empty to prevent build errors
// if any stale imports exist.

export default function DemoRoleSwitcher() {
  return null;
}
